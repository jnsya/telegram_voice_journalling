[phases.setup]
nixPkgs = ['ffmpeg', 'python310', 'python310Packages.pip']

[phases.install]
cmds = ['python3 -m ensurepip --upgrade && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt']

[start]
cmd = 'python3 voice_bot.py'